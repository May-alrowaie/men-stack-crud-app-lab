<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= plant.name %></title>
  </head>
  <body>
    <h1><%= plant.name %></h1>

    <% if (plant.isReadyToEat) { %>
    <p>This plant is ready to eat!</p>
    <% } else { %>
    <p>This plant is not ready to eat!</p>
    <% } %>

    <form action="/plants/<%=plant._id%>?_method=DELETE" method="POST">
      <button type="submit">Delete <%= plant.name %></button>
      <a href="/plants/<%=plant._id%>/edit">Edit</a>
      <a href="/plants">Back to Plants</a>
    </form>
  </body>
</html>
