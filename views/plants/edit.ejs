<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit <%= plant.name%></title>
  </head>
  <body>
    <a href="/plants/">Back to Plants</a>

    <h1>Create a New Plant!</h1>
    <form action="/plants/<%= plant.id%>" method="POST">
      <label for="name">Name:</label>
      <input type="text" name="name" id="name" value="<%= plant.name%>" />

      <label for="isReadyToEat">Ready to Plant?</label>
      <input type="checkbox" name="isReadyToEat" id="isReadyToEat" <%=
      plant.isReadyToEat? 'checked':""%>/>

      <button type="submit">Add Plant</button>
    </form>
  </body>
</html>
