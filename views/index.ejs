<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Plants App</title>
  </head>

  <body>
    <h1><%= plant.name %></h1>

    <% if (plant.isReadyToEat) { %>
    <p>This plant is ready to eat!</p>
    <% } else { %>
    <p>This plant is not ready to eat!</p>
    <% } %>
  </body>
</html>
